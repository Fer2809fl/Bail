import{createCipheriv,createDecipheriv,createHash,createHmac,randomBytes}from"crypto";import*as curve from"libsignal/src/curve.js";import{KEY_BUNDLE_TYPE}from"../Defaults/index.js";import{md5,hkdf}from"whatsapp-rust-bridge";const{subtle}=globalThis.crypto,generateSignalPubKey=pubKey=>pubKey.length===33?pubKey:Buffer.concat([KEY_BUNDLE_TYPE,pubKey]),Curve={generateKeyPair:()=>{const{pubKey,privKey}=curve.generateKeyPair();return{private:Buffer.from(privKey),public:Buffer.from(pubKey.slice(1))}},sharedKey:(privateKey,publicKey)=>{const shared=curve.calculateAgreement(generateSignalPubKey(publicKey),privateKey);return Buffer.from(shared)},sign:(privateKey,buf)=>curve.calculateSignature(privateKey,buf),verify:(pubKey,message,signature)=>{try{return curve.verifySignature(generateSignalPubKey(pubKey),message,signature),!0}catch{return!1}}},signedKeyPair=(identityKeyPair,keyId)=>{const preKey=Curve.generateKeyPair(),pubKey=generateSignalPubKey(preKey.public),signature=Curve.sign(identityKeyPair.private,pubKey);return{keyPair:preKey,signature,keyId}},GCM_TAG_LENGTH=16;function aesEncryptGCM(plaintext,key,iv,additionalData){const cipher=createCipheriv("aes-256-gcm",key,iv);return cipher.setAAD(additionalData),Buffer.concat([cipher.update(plaintext),cipher.final(),cipher.getAuthTag()])}function aesDecryptGCM(ciphertext,key,iv,additionalData){const decipher=createDecipheriv("aes-256-gcm",key,iv),enc=ciphertext.slice(0,ciphertext.length-GCM_TAG_LENGTH),tag=ciphertext.slice(ciphertext.length-GCM_TAG_LENGTH);return decipher.setAAD(additionalData),decipher.setAuthTag(tag),Buffer.concat([decipher.update(enc),decipher.final()])}function aesEncryptCTR(plaintext,key,iv){const cipher=createCipheriv("aes-256-ctr",key,iv);return Buffer.concat([cipher.update(plaintext),cipher.final()])}function aesDecryptCTR(ciphertext,key,iv){const decipher=createDecipheriv("aes-256-ctr",key,iv);return Buffer.concat([decipher.update(ciphertext),decipher.final()])}function aesDecrypt(buffer,key){return aesDecryptWithIV(buffer.subarray(16),key,buffer.subarray(0,16))}function aesDecryptWithIV(buffer,key,IV){const aes=createDecipheriv("aes-256-cbc",key,IV);return Buffer.concat([aes.update(buffer),aes.final()])}function aesEncrypt(buffer,key){const IV=randomBytes(16),aes=createCipheriv("aes-256-cbc",key,IV);return Buffer.concat([IV,aes.update(buffer),aes.final()])}function aesEncrypWithIV(buffer,key,IV){const aes=createCipheriv("aes-256-cbc",key,IV);return Buffer.concat([aes.update(buffer),aes.final()])}function hmacSign(buffer,key,variant="sha256"){return createHmac(variant,key).update(buffer).digest()}function sha256(buffer){return createHash("sha256").update(buffer).digest()}async function derivePairingCodeKey(pairingCode,salt){const pairingCodeBuffer=new TextEncoder().encode(pairingCode),saltBuffer=new Uint8Array(salt instanceof Uint8Array?salt:new Uint8Array(salt)),keyMaterial=await subtle.importKey("raw",pairingCodeBuffer,{name:"PBKDF2"},!1,["deriveBits"]),derivedBits=await subtle.deriveBits({name:"PBKDF2",salt:saltBuffer,iterations:2<<16,hash:"SHA-256"},keyMaterial,256);return Buffer.from(derivedBits)}export{Curve,aesDecrypt,aesDecryptCTR,aesDecryptGCM,aesDecryptWithIV,aesEncrypWithIV,aesEncrypt,aesEncryptCTR,aesEncryptGCM,derivePairingCodeKey,generateSignalPubKey,hkdf,hmacSign,md5,sha256,signedKeyPair};
