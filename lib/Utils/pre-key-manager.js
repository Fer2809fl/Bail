import PQueue from"p-queue";class PreKeyManager{constructor(store,logger){this.store=store,this.logger=logger,this.queues=new Map}getQueue(keyType){return this.queues.has(keyType)||this.queues.set(keyType,new PQueue({concurrency:1})),this.queues.get(keyType)}async processOperations(data,keyType,transactionCache,mutations,isInTransaction){const keyData=data[keyType];if(keyData)return this.getQueue(keyType).add(async()=>{transactionCache[keyType]=transactionCache[keyType]||{},mutations[keyType]=mutations[keyType]||{};const deletions=[],updates={};for(const keyId in keyData)keyData[keyId]===null?deletions.push(keyId):updates[keyId]=keyData[keyId];Object.keys(updates).length>0&&(Object.assign(transactionCache[keyType],updates),Object.assign(mutations[keyType],updates)),deletions.length>0&&await this.processDeletions(keyType,deletions,transactionCache,mutations,isInTransaction)})}async processDeletions(keyType,ids,transactionCache,mutations,isInTransaction){if(isInTransaction)for(const keyId of ids)transactionCache[keyType]?.[keyId]?(transactionCache[keyType][keyId]=null,mutations[keyType][keyId]=null):this.logger.warn(`Skipping deletion of non-existent ${keyType} in transaction: ${keyId}`);else{const existingKeys=await this.store.get(keyType,ids);for(const keyId of ids)existingKeys[keyId]?(transactionCache[keyType][keyId]=null,mutations[keyType][keyId]=null):this.logger.warn(`Skipping deletion of non-existent ${keyType}: ${keyId}`)}}async validateDeletions(data,keyType){const keyData=data[keyType];if(keyData)return this.getQueue(keyType).add(async()=>{const deletionIds=Object.keys(keyData).filter(id=>keyData[id]===null);if(deletionIds.length===0)return;const existingKeys=await this.store.get(keyType,deletionIds);for(const keyId of deletionIds)existingKeys[keyId]||(this.logger.warn(`Skipping deletion of non-existent ${keyType}: ${keyId}`),delete data[keyType][keyId])})}}export{PreKeyManager};
