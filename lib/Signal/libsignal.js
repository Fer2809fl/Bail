import*as libsignal from"libsignal";import{PreKeyWhisperMessage}from"libsignal/src/protobufs.js";import{LRUCache}from"lru-cache";import{generateSignalPubKey}from"../Utils/index.js";import{isHostedLidUser,isHostedPnUser,isLidUser,isPnUser,jidDecode,transferDevice,WAJIDDomains}from"../WABinary/index.js";import{SenderKeyName}from"./Group/sender-key-name.js";import{SenderKeyRecord}from"./Group/sender-key-record.js";import{GroupCipher,GroupSessionBuilder,SenderKeyDistributionMessage}from"./Group/index.js";import{LIDMappingStore}from"./lid-mapping.js";function extractIdentityFromPkmsg(ciphertext){try{if(!ciphertext||ciphertext.length<2||(ciphertext[0]&15)!==3)return;const preKeyProto=PreKeyWhisperMessage.decode(ciphertext.slice(1));return preKeyProto.identityKey?.length===33?new Uint8Array(preKeyProto.identityKey):void 0}catch{return}}function makeLibSignalRepository(auth,logger,pnToLIDFunc){const lidMapping=new LIDMappingStore(auth.keys,logger,pnToLIDFunc),storage=signalStorage(auth,lidMapping),parsedKeys=auth.keys,migratedSessionCache=new LRUCache({ttl:4320*60*1e3,ttlAutopurge:!0,updateAgeOnGet:!0});return{decryptGroupMessage({group,authorJid,msg}){const senderName=jidToSignalSenderKeyName(group,authorJid),cipher=new GroupCipher(storage,senderName);return parsedKeys.transaction(async()=>cipher.decrypt(msg),group)},async processSenderKeyDistributionMessage({item,authorJid}){const builder=new GroupSessionBuilder(storage);if(!item.groupId)throw new Error("Group ID is required for sender key distribution message");const senderName=jidToSignalSenderKeyName(item.groupId,authorJid),senderMsg=new SenderKeyDistributionMessage(null,null,null,null,item.axolotlSenderKeyDistributionMessage),senderNameStr=senderName.toString(),{[senderNameStr]:senderKey}=await auth.keys.get("sender-key",[senderNameStr]);return senderKey||await storage.storeSenderKey(senderName,new SenderKeyRecord),parsedKeys.transaction(async()=>{const{[senderNameStr]:senderKey2}=await auth.keys.get("sender-key",[senderNameStr]);senderKey2||await storage.storeSenderKey(senderName,new SenderKeyRecord),await builder.process(senderName,senderMsg)},item.groupId)},async decryptMessage({jid,type,ciphertext}){const addr=jidToSignalProtocolAddress(jid),session=new libsignal.SessionCipher(storage,addr);if(type==="pkmsg"){const identityKey=extractIdentityFromPkmsg(ciphertext);if(identityKey){const addrStr=addr.toString();await storage.saveIdentity(addrStr,identityKey)&&logger.info({jid,addr:addrStr},"identity key changed or new contact, session will be re-established")}}async function doDecrypt(){let result;switch(type){case"pkmsg":result=await session.decryptPreKeyWhisperMessage(ciphertext);break;case"msg":result=await session.decryptWhisperMessage(ciphertext);break}return result}return parsedKeys.transaction(async()=>await doDecrypt(),jid)},async encryptMessage({jid,data}){const addr=jidToSignalProtocolAddress(jid),cipher=new libsignal.SessionCipher(storage,addr);return parsedKeys.transaction(async()=>{const{type:sigType,body}=await cipher.encrypt(data);return{type:sigType===3?"pkmsg":"msg",ciphertext:Buffer.from(body,"binary")}},jid)},async encryptGroupMessage({group,meId,data}){const senderName=jidToSignalSenderKeyName(group,meId),builder=new GroupSessionBuilder(storage),senderNameStr=senderName.toString();return parsedKeys.transaction(async()=>{const{[senderNameStr]:senderKey}=await auth.keys.get("sender-key",[senderNameStr]);senderKey||await storage.storeSenderKey(senderName,new SenderKeyRecord);const senderKeyDistributionMessage=await builder.create(senderName);return{ciphertext:await new GroupCipher(storage,senderName).encrypt(data),senderKeyDistributionMessage:senderKeyDistributionMessage.serialize()}},group)},async injectE2ESession({jid,session}){logger.trace({jid},"injecting E2EE session");const cipher=new libsignal.SessionBuilder(storage,jidToSignalProtocolAddress(jid));return parsedKeys.transaction(async()=>{await cipher.initOutgoing(session)},jid)},jidToSignalProtocolAddress(jid){return jidToSignalProtocolAddress(jid).toString()},lidMapping,async validateSession(jid){try{const addr=jidToSignalProtocolAddress(jid),session=await storage.loadSession(addr.toString());return session?session.haveOpenSession()?{exists:!0}:{exists:!1,reason:"no open session"}:{exists:!1,reason:"no session"}}catch{return{exists:!1,reason:"validation error"}}},async deleteSession(jids){if(!jids.length)return;const sessionUpdates={};return jids.forEach(jid=>{const addr=jidToSignalProtocolAddress(jid);sessionUpdates[addr.toString()]=null}),parsedKeys.transaction(async()=>{await auth.keys.set({session:sessionUpdates})},`delete-${jids.length}-sessions`)},async migrateSession(fromJid,toJid){if(!fromJid||!isLidUser(toJid)&&!isHostedLidUser(toJid))return{migrated:0,skipped:0,total:0};if(!isPnUser(fromJid)&&!isHostedPnUser(fromJid))return{migrated:0,skipped:0,total:1};const{user}=jidDecode(fromJid);logger.debug({fromJid},"bulk device migration - loading all user devices");const{[user]:userDevices}=await parsedKeys.get("device-list",[user]);if(!userDevices)return{migrated:0,skipped:0,total:0};const{device:fromDevice}=jidDecode(fromJid),fromDeviceStr=fromDevice?.toString()||"0";userDevices.includes(fromDeviceStr)||userDevices.push(fromDeviceStr);const deviceSessionKeys=userDevices.filter(device=>{const deviceKey=`${user}.${device}`;return!migratedSessionCache.has(deviceKey)}).map(device=>`${user}.${device}`),existingSessions=await parsedKeys.get("session",deviceSessionKeys),deviceJids=[];for(const[sessionKey,sessionData]of Object.entries(existingSessions))if(sessionData){const deviceStr=sessionKey.split(".")[1];if(!deviceStr)continue;const deviceNum=parseInt(deviceStr);let jid=deviceNum===0?`${user}@s.whatsapp.net`:`${user}:${deviceNum}@s.whatsapp.net`;deviceNum===99&&(jid=`${user}:99@hosted`),deviceJids.push(jid)}return logger.debug({fromJid,totalDevices:userDevices.length,devicesWithSessions:deviceJids.length,devices:deviceJids},"bulk device migration complete - all user devices processed"),parsedKeys.transaction(async()=>{const migrationOps=deviceJids.map(jid=>{const lidWithDevice=transferDevice(jid,toJid),fromDecoded=jidDecode(jid),toDecoded=jidDecode(lidWithDevice);return{fromJid:jid,toJid:lidWithDevice,pnUser:fromDecoded.user,lidUser:toDecoded.user,deviceId:fromDecoded.device||0,fromAddr:jidToSignalProtocolAddress(jid),toAddr:jidToSignalProtocolAddress(lidWithDevice)}}),totalOps=migrationOps.length;let migratedCount=0;const pnAddrStrings=Array.from(new Set(migrationOps.map(op=>op.fromAddr.toString()))),pnSessions=await parsedKeys.get("session",pnAddrStrings),sessionUpdates={};for(const op of migrationOps){const pnAddrStr=op.fromAddr.toString(),lidAddrStr=op.toAddr.toString(),pnSession=pnSessions[pnAddrStr];if(pnSession){const fromSession=libsignal.SessionRecord.deserialize(pnSession);fromSession.haveOpenSession()&&(sessionUpdates[lidAddrStr]=fromSession.serialize(),sessionUpdates[pnAddrStr]=null,migratedCount++)}}if(Object.keys(sessionUpdates).length>0){await parsedKeys.set({session:sessionUpdates}),logger.debug({migratedSessions:migratedCount},"bulk session migration complete");for(const op of migrationOps)if(sessionUpdates[op.toAddr.toString()]){const deviceKey=`${op.pnUser}.${op.deviceId}`;migratedSessionCache.set(deviceKey,!0)}}const skippedCount=totalOps-migratedCount;return{migrated:migratedCount,skipped:skippedCount,total:totalOps}},`migrate-${deviceJids.length}-sessions-${jidDecode(toJid)?.user}`)}}}const jidToSignalProtocolAddress=jid=>{const decoded=jidDecode(jid),{user,device,server,domainType}=decoded;if(!user)throw new Error(`JID decoded but user is empty: "${jid}" -> user: "${user}", server: "${server}", device: ${device}`);const signalUser=domainType!==WAJIDDomains.WHATSAPP?`${user}_${domainType}`:user,finalDevice=device||0;if(device===99&&decoded.server!=="hosted"&&decoded.server!=="hosted.lid")throw new Error("Unexpected non-hosted device JID with device 99. This ID seems invalid. ID:"+jid);return new libsignal.ProtocolAddress(signalUser,finalDevice)},jidToSignalSenderKeyName=(group,user)=>new SenderKeyName(group,jidToSignalProtocolAddress(user));function signalStorage({creds,keys},lidMapping){const resolveLIDSignalAddress=async id=>{if(id.includes(".")){const[deviceId,device]=id.split("."),[user,domainType_]=deviceId.split("_"),domainType=parseInt(domainType_||"0");if(domainType===WAJIDDomains.LID||domainType===WAJIDDomains.HOSTED_LID)return id;const pnJid=`${user}${device!=="0"?`:${device}`:""}@${domainType===WAJIDDomains.HOSTED?"hosted":"s.whatsapp.net"}`,lidForPN=await lidMapping.getLIDForPN(pnJid);if(lidForPN)return jidToSignalProtocolAddress(lidForPN).toString()}return id};return{loadSession:async id=>{try{const wireJid=await resolveLIDSignalAddress(id),{[wireJid]:sess}=await keys.get("session",[wireJid]);if(sess)return libsignal.SessionRecord.deserialize(sess)}catch{return null}return null},storeSession:async(id,session)=>{const wireJid=await resolveLIDSignalAddress(id);await keys.set({session:{[wireJid]:session.serialize()}})},isTrustedIdentity:()=>!0,loadIdentityKey:async id=>{const wireJid=await resolveLIDSignalAddress(id),{[wireJid]:key}=await keys.get("identity-key",[wireJid]);return key||void 0},saveIdentity:async(id,identityKey)=>{const wireJid=await resolveLIDSignalAddress(id),{[wireJid]:existingKey}=await keys.get("identity-key",[wireJid]),keysMatch=existingKey&&existingKey.length===identityKey.length&&existingKey.every((byte,i)=>byte===identityKey[i]);return existingKey&&!keysMatch?(await keys.set({session:{[wireJid]:null},"identity-key":{[wireJid]:identityKey}}),!0):existingKey?!1:(await keys.set({"identity-key":{[wireJid]:identityKey}}),!0)},loadPreKey:async id=>{const keyId=id.toString(),{[keyId]:key}=await keys.get("pre-key",[keyId]);if(key)return{privKey:Buffer.from(key.private),pubKey:Buffer.from(key.public)}},removePreKey:id=>keys.set({"pre-key":{[id]:null}}),loadSignedPreKey:()=>{const key=creds.signedPreKey;return{privKey:Buffer.from(key.keyPair.private),pubKey:Buffer.from(key.keyPair.public)}},loadSenderKey:async senderKeyName=>{const keyId=senderKeyName.toString(),{[keyId]:key}=await keys.get("sender-key",[keyId]);return key?SenderKeyRecord.deserialize(key):new SenderKeyRecord},storeSenderKey:async(senderKeyName,key)=>{const keyId=senderKeyName.toString(),serialized=JSON.stringify(key.serialize());await keys.set({"sender-key":{[keyId]:Buffer.from(serialized,"utf-8")}})},getOurRegistrationId:()=>creds.registrationId,getOurIdentity:()=>{const{signedIdentityKey}=creds;return{privKey:Buffer.from(signedIdentityKey.private),pubKey:Buffer.from(generateSignalPubKey(signedIdentityKey.public))}}}}export{makeLibSignalRepository};
